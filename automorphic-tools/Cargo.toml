[package]
name = "automorphic-tools"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "automorphic-tools"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
walkdir = "2.3"
regex = "1.5"
clap = { version = "4.0", features = ["derive"] }
