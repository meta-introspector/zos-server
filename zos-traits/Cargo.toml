[package]
name = "zos-traits"
version = "0.1.0"
edition = "2021"
description = "ZOS Core Traits - Zero dependency trait foundation"
license = "AGPL-3.0"

[dependencies]
# Zero dependencies - pure traits only
